command /bc [<text>]:
    permission: op
    trigger:
        if arg-1 is set:
            broadcast "&f&l[&a&lDaddy&b&lBox&f&l] &8&l» &f%colored arg-1%"
        else:
            send "&c/bc (message)" 
command /broadcast [<text>]:
    permission: op
    trigger:
        if arg-1 is set:
            broadcast "&f&l[&a&lDaddy&b&lBox&f&l] &8&l» &f%colored arg-1%"
        else:
            send "&c/bc (message)" 
            send "&c/broadcast (message)" 